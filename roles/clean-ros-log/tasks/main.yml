---
- name: Copy script
  ansible.builtin.copy:
    src: run.sh
    dest: /opt/autoware/services/clean-ros-log/
    mode: 0755
  become: yes

- name: Copy service
  ansible.builtin.copy:
    src: clean-ros-log.service
    dest: /etc/systemd/system/
    mode: 0755
  become: yes

- name: Enable service
  ansible.builtin.systemd:
    name: clean-ros-log.service
    daemon_reload: "{{ daemon_reload }}"
    enabled: yes
    masked: no
  become: yes
