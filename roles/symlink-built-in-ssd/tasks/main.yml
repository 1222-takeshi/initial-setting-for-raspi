---
- name: Copy script
  ansible.builtin.copy:
    src: run.sh
    dest: /opt/autoware/services/symlink-built-in-ssd/
    mode: 0755
  become: yes

- name: Copy service
  ansible.builtin.copy:
    src: symlink-built-in-ssd.service
    dest: /etc/systemd/system/
    mode: 0755
  become: yes

- name: Enable service
  ansible.builtin.systemd:
    name: symlink-built-in-ssd.service
    daemon_reload: "{{ daemon_reload }}"
    enabled: yes
    masked: no
  become: yes
